// Helper: Cloudinary media uploads
// Used for: student photos, video answers, audio answers, class photo, AI-generated image

export async function uploadMedia(file: Buffer, folder: string, resourceType: 'image' | 'video' | 'raw' = 'image'): Promise<string> {
  // TODO: use Cloudinary Node SDK to upload, return secure_url
  return ''
}
